#include <iostream>
#include <iomanip> 
#include "windows.h"
using namespace std;
int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	float minX, maxX, h, x;
	double y;
	cout << "Введіть мінімальне значення х: ";
	cin >> minX;
	cout << "Введіть максимальне значення х: ";
	cin >> maxX;
	cout << "Введіть крок за х: ";
	cin >> h;
	x = minX;
	cout << setw(10) << left << "х=" << setw(4) << "у=\n";
	while (x <= maxX + (h / 2))
	{
		y = pow(x, 9)+pow(x,1./2)-4;
		cout << setw(10) << left << x  << setw(9) << y << endl ;//ent
		x += h;
	}
system("pause");
	return 0;
}
